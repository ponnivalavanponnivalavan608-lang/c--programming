#include <stdio.h>
int main() {
    int a[5]={1,2,3,4,5}, key=3, found=0;
    for(int i=0;i<5;i++){
        if(a[i]==key){
            found=1;
            printf("Found at %d", i);
            break;
        }
    }
    if(!found) printf("Not Found");
}
