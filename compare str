#include <stdio.h>
int main() {
    char s1[]="abc", s2[]="abc";
    int i=0, flag=1;
    while(s1[i]!='\0' || s2[i]!='\0'){
        if(s1[i]!=s2[i]){
            flag=0; break;
        }
        i++;
    }
    if(flag) printf("Equal");
    else printf("Not Equal");
}
